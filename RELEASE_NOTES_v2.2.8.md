# v2.2.8 — final correction pass

This release makes a surgical presentation, attribution, and synchronization
update to the v2.2.7 manuscript. Its mathematical conclusions and formalization
scope are unchanged.

## Corrections

- separates the global degree and diameter obstructions from the
  diameter-three score identity in the abstract;
- states the exact open adjacency-spectral window in the introduction;
- gives precise attribution for Backelin's ball-count identity and for
  minimality and uniqueness of the Anstee--Robertson graph;
- defines the cubic quotient factor used in the order-50 local argument;
- records the missing nonnegativity conditions in the endpoint proof and
  invokes Sylvester's law of inertia explicitly in the six-deletion witness;
- improves four page transitions without changing the one-inch margins;
- preserves the abstract in the generated Markdown reading copy;
- synchronizes the cover-letter template, provenance boundary, proof-audit
  registry, release metadata, PDF, arXiv source archive, manifest, and checksum
  ledger.

The deterministic arXiv archive contains only `main.tex`, `references.bib`,
and `main.bbl`. The authoritative file digests are recorded in `SHA256SUMS`.
